{
  "name": "boomerang-travel-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix frontend && (cd frontend && npx vite build) && npm install --prefix admin && (cd admin && npx vite build) && node build.js",
    "dev:backend": "php -S localhost:8000 -t .",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "dev": "start \"PHP Backend\" cmd /k npm run dev:backend & start \"Frontend\" cmd /k npm run dev:frontend & start \"Admin\" cmd /k npm run dev:admin"
  }
}
