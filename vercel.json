{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/admin(/.*)?", "dest": "/admin/index.html" },
    { "src": "/(.*)",        "dest": "/index.html" }
  ]
}
